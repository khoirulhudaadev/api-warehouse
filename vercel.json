{
    "version": 2,
    "builds": [
        {
            "src": "api/index.php",
            "use": "@vercel/php",
            "config": { "runtime": "php-8.0" }
        }
    ],
    "buildCommand": "apt-get update && apt-get install -y libssl-dev && vite build",
    "outputDirectory": "public"
}
